<%- submit_btn_css ||= 'btn btn-link' %>
<%= form_tag oauth_application_path(application) do %>
  <input type="hidden" name="_method" value="delete">
  <%= submit_tag t('doorkeeper.views.applications.buttons.destroy'), onclick: "return confirm('#{ t('doorkeeper.views.applications.confirmations.destroy') }')", class: submit_btn_css %>
<% end %>
